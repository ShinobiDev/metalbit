ALTER TABLE `detalle_recargas` ADD `estado_detalle_recarga` ENUM('APROVADA','PENDIENTE') NOT NULL DEFAULT 'APROVADA' AFTER `referencia_pago`;
ALTER TABLE `pagos` ADD `estado_pago` ENUM('APROVADA','PENDIENTE') NOT NULL DEFAULT 'APROVADA' AFTER `id_user_compra`;
ALTER TABLE `recargas` ADD UNIQUE(`user_id`);
ALTER TABLE `detalle_recargas` CHANGE `estado_detalle_recarga` `estado_detalle_recarga` ENUM('APROVADA','PENDIENTE','RECHAZADA') CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci NOT NULL DEFAULT 'APROVADA';
ALTER TABLE `detalle_recargas` ADD `referencia_pago_pay_u` VARCHAR(256) NOT NULL AFTER `referencia_pago`;
ALTER TABLE `detalle_recargas` ADD `metodo_pago` VARCHAR(256) NOT NULL AFTER `referencia_pago_pay_u`
ALTER TABLE `pagos` ADD `metodo_pago` VARCHAR(256) NOT NULL AFTER `id_user_compra`;
ALTER TABLE `pagos` CHANGE `estado_pago` `estado_pago` ENUM('APROVADA','PENDIENTE','RECHAZADA') CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'APROVADA';

	